/* --------------------- FOR LOOP PRACTICE --------------------- */

// Finish the code below to print out the correct iteration
// e.g. first log should say "This is iteration number 1"
// e.g. second log should say "This is iteration number 2" etc.

for (let i = 0; i < 10; i++) {
  console.log('This is iteration number')
}

/* --------------------- FOR OF LOOP PRACTICE --------------------- */

// 1. Declare a variable and initialize it with an array containing five values of any data type

// 2. Write a for... of loop and print the values to the console

/* --------------------- ARRAY METHOD PRACTICE --------------------- */

// 1. Use the array from the previous exercise and remove the first value from the beginning
// 2. Add a new value to the end of the array
// 3. Loop through the values and print to screen
// 4. Call the function

function practiceArrMethods() {}

/* --------------------- MORE ARRAY METHODS --------------------- */

// 1. Concatenate the arrays
// 2. Reverse the array
// 3. Join the string with ''
// 4. Print the result to the console
// 4. Call the function

const arr1 = ['t', 'p', 'i', 'r', 'c', 's', 'a', 'v', 'a', 'j', ' ', 'e']
const arr2 = ['v', 'o', 'l', ' ', 'I']

function concatArr() {}

// Copy the code for the splice and slice methods below and print the results to the console.

/* --------------------- STRING LOOP--------------------- */

// Declare a variable and initialize it with a string of your choice.
// Using a for... of loop, loop through each value and print it to the console.

/* --------------------- DOM MANIPULATION & MOUSE EVENTS --------------------- */

/* Print contents of an HTML to the console.
1. Declare a variable
2. Assign the element with an ID in index.html to the variable
3. Print its contents to the console (hint: Google innerText)
*/

/* Add a button to the document and remove any node when it is clicked */

/* --------------------- SET TIMEOUT AND SET INTERVAL --------------------- */

// Log any string to the console using setTimeout.

// Log any data type to the console every 2 seconds using setInterval.

// Add another button to the document and clear the interval on click.

